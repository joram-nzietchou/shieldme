import 'package:flutter/material.dart';

class SmsTab extends StatelessWidget {
  const SmsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Page SMS - À compléter'),
    );
  }
}