import 'package:flutter/material.dart';

class PremiumTab extends StatelessWidget {
  const PremiumTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Page Premium - À compléter'),
    );
  }
}